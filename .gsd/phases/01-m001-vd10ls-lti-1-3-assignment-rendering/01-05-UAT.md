# S05: AGS Grade Passback to Canvas — UAT

**Milestone:** M001
**Written:** 2026-07-15
**Status:** deferred — human verification pending; live-Canvas checks BLOCKED on UTD developer key

| # | Check | Verdict | How to test |
|---|-------|---------|-------------|
| 1 | Submit records pending passback intent | PENDING | Student submits → TA dashboard Gradebook column shows "pending" chip |
| 2 | Post grade (dry run) succeeds | PENDING | AGS_MODE=dry_run (default): click Post on a row → chip becomes "dry run" (tooltip explains nothing sent) |
| 3 | Post All Grades posts latest per student | PENDING | Two students + resubmits → Post All → each student's newest submission gets a chip |
| 4 | Effective score is what posts | PENDING | Verify checkpoints first, then Post → grade_passbacks.score_given = auto + checkpoint points (inspect DB or dry-run tooltip) |
| 5 | Failure surfaces with error + Retry | PENDING (needs live/misconfig) | Set AGS_MODE=live with bogus LTI_TOKEN_URL → "⚠ failed (n)" chip with error tooltip, button reads Retry |
| 6 | Automated coverage | PASS | 13 unit tests: AGS payload/token shape, retry-then-succeed, exhaustion, 4xx no-retry (score+token), network retries, mode gating, chip renders |

## Blocked until UTD Canvas developer key exists

| # | Check | Notes |
|---|-------|-------|
| B1 | Real OIDC launch stores AGS lineitem claim | /lti/launch is a placeholder until the key exists |
| B2 | Live score post reaches Canvas gradebook | AGS_MODE=live + LTI_TOKEN_URL + LTI_TOOL_PRIVATE_KEY |
| B3 | Manual posting policy holds grades until instructor release | Canvas-side gradebook setting, verify in test course |
