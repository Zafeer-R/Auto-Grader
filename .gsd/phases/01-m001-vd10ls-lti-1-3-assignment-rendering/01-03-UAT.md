# S03: Data Table Grading (R1, R2, R3) — UAT

**Milestone:** M001
**Written:** 2026-07-15
**Status:** deferred — human verification pending (UATs batched to end of autonomous run per user)

Setup: `docker compose up -d && alembic upgrade head && uvicorn app.main:app --reload`, then http://localhost:8000/dev/launch

| # | Check | Verdict | How to test |
|---|-------|---------|-------------|
| 1 | R1/R2/R3 render as editable grids in the Lab01 form | PENDING | Scroll to Table R1 (after Measurement), R2/R3 (after Check Box #2); 20+16+8 input cells |
| 2 | Consistent submission earns full table credit | PENDING | Enter 5 close trials per R1 row; enter matching stats in R2 (0.001 cm / 0.01 g precision) and V/D in R3; expect 24/24 on tables, no ⚠ |
| 3 | Inconsistent R2 value keeps points but shows ⚠ | PENDING | Enter an SD wildly different from your R1 spread (right precision): score unchanged, ⚠ badge + amber flag panel citing your R1-derived value |
| 4 | Precision enforced per cell | PENDING | Enter "5.1" as a length mean (needs 0.001 cm): cell ✗ "must be expressed to 3 decimal places" |
| 5 | Missing R1 degrades gracefully | PENDING | Leave R1 blank, fill R2/R3: cells still grade, flag says "could not verify against Table R1 data" |
| 6 | Nominal anchors catch wild means | PENDING | Enter density mean 8.90 (copper, not aluminum): cell ✗ |
| 7 | Automated coverage | PASS | 12 unit tests (TestR1Stats, TestGradeDataTables), template smoke renders |

**Instructor input needed:** confirm/tune nominal tile values + tolerances in `answer_keys/lab01.json` (`tables` section).
